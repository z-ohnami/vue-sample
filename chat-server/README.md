
```
yarn global add firebase-tools
firebase login
firebase init hosting
firebase init functions
npm i express cors
npm install
firebase serve --only functions
firebase deploy --only functions
```

```
curl -XPOST -H'Content-Type:application/json' -d'{"cname":"general"}'  http://localhost:5000/{$project-id}/us-central1/v1/channels
curl http://localhost:5000/{$project-id}/us-central1/v1/channels
curl -XPOST -H'Content-Type:application/json' -d'{}' https://us-central1-{$project-id}.cloudfunctions.net/v1/reset
```
